<h2>Users you may connect with:</h2>

<table>
  <tr>
    <th>Name</th>
    <th>Send Invite?</th>
  </tr>
  <% @users.each do |person| %>
    <tr>
      <td><a href="/users/<%= person.id %>"><%= person.name %></a></td>
      <td><a href="/profiles/new">Invite</a></td>
    </tr>
  <% end %>
</table>
